# ============================================================
# RAG Pipeline — Requirements
# Install: pip install -r requirements.txt
# Python 3.11+ required
# Pinned to exact versions verified working together on 2026-07-31.
# ============================================================

# ── Core / Config ────────────────────────────────────────────
pyyaml==6.0.3
pydantic==2.13.4
python-dotenv==1.2.1
psutil==7.1.2
requests==2.32.5

# ── Connectors ────────────────────────────────────────────────
# PDF (digital + OCR fallback)
pymupdf==1.26.5           # fitz — fast PDF parsing
pymupdf4llm==0.0.27       # markdown extraction helper
pytesseract==0.3.13       # OCR (requires the Tesseract binary separately)
pillow==11.3.0

# Word documents (.docx, .doc)
python-docx==1.2.0

# PowerPoint presentations (.pptx, .ppt)
python-pptx==1.0.2

# Excel / CSV
pandas==2.3.3
openpyxl==3.1.5
xlrd==2.0.2                # legacy .xls support

# Email — mbox (stdlib) + PST
# libpff-python — install separately if PST support needed:
# pip install libpff-python  (may require build tools on Windows)

# Chat exports (WhatsApp, Telegram, Slack, Teams) — stdlib json + regex

# Audio transcription + diarization
faster-whisper==1.2.1
# pyannote-audio>=3.1.1   # optional; requires HuggingFace token
#   install separately: pip install pyannote-audio

# ── Chunking ─────────────────────────────────────────────────
langchain-text-splitters==0.3.11

# ── Structured DB ────────────────────────────────────────────
# sqlite3 — stdlib
# pandas already listed above

# ── Embedder ─────────────────────────────────────────────────
sentence-transformers==5.1.2
torch==2.8.0               # CPU build; use torch+cu121 for CUDA
# For CPU-only (smaller install):
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Vector Store ─────────────────────────────────────────────
chromadb==1.5.9
faiss-cpu==1.13.0          # swap to faiss-gpu if CUDA available

# ── Retrieval + Generation ───────────────────────────────────
rank-bm25==0.2.2           # BM25 hybrid retrieval
httpx==0.28.1              # async Ollama streaming + FastAPI test client
# tiktoken — optional, for token counting
# tiktoken>=0.7.0

# ── Knowledge Graph ──────────────────────────────────────────
networkx==3.2.1
python-louvain==0.16       # imported as `community`

# ── API Layer ────────────────────────────────────────────────
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart==0.0.20   # for file upload endpoints
sse-starlette==3.3.0       # Server-Sent Events for streaming

# ── Incremental Sync ─────────────────────────────────────────
apscheduler==3.11.3

# ── Tests ────────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==1.2.0

# ── Utilities ────────────────────────────────────────────────
rich==15.0.0                # pretty console output
tqdm==4.68.3                 # progress bars
python-dateutil==2.9.0.post0
chardet==5.2.0               # encoding detection for text files
